import Button from "@mui/material/Button";

const CreateTask = () => {
  return (
    <Button variant="contained" sx={{ width: { xs: "100%", md: "20%" } }}>
      Create
    </Button>
  );
};

export default CreateTask;
